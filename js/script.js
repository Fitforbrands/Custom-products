const appOptions = {
  stageWidth: 600,
  stageHeight: 900,
  fileServerURL: "../img/book-prowork.png",
};

const fpd = new FancyProductDesigner(
  document.getElementById("fpd-target"),
  appOptions
);
